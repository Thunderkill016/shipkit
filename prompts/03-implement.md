# Implement an accepted plan

Read `AGENTS.md`, the issue, and the accepted plan.

Implement only the next coherent slice. Preserve behavior outside the
acceptance criteria. Do not add dependencies or alter architecture unless the
plan explicitly permits it. Add or update meaningful tests.

Run focused checks while iterating, then run `pnpm verify`. Review the final
diff for accidental scope expansion. Report behavior, files, tests, command
outcomes, assumptions, remaining risks, and manual review points. Do not claim
completion if a required check fails.
