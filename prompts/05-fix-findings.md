# Fix accepted review findings

Read `AGENTS.md`, the accepted findings, issue, plan, and current diff.

Fix only accepted findings. Do not use the review as permission for unrelated
cleanup. Add a regression test where practical. Run focused checks and
`pnpm verify`, then report each finding as fixed, rejected with reason, or still
blocked.
