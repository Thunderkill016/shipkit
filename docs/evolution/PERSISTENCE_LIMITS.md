# Persistence Limits

PR #22 is limited to Ubuntu 24.04 / Node.js 22 process-crash recovery on a local filesystem with atomic rename and file/directory `fsync`. Cross-platform, power-loss, network-filesystem, hostile-writer, and future-schema guarantees remain separate work.
