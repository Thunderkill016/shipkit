{
  "name": "@shipkit/logger",
  "version": "0.1.0",
  "description": "Structured logging port for Shipkit — console fallback, Pino/Sentry-ready",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4"
  }
}
