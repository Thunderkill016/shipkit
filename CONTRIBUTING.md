# Contributing

1. Read `DECISIONS.md` and `ARCHITECTURE.md` first.  
2. Keep vendor SDKs in adapters.  
3. Prefer tests for security/authz behavior.  
4. One idea per PR.  

```bash
pnpm install
pnpm doctor
pnpm dev
pnpm --filter @shipkit/web build
```
